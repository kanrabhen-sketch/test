rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 사내 업무 시스템: 로그인한 사용자만 접근 허용
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
